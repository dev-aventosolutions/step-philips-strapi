'use strict';

/**
 * contact-tenant-card router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contact-tenant-card.contact-tenant-card');
