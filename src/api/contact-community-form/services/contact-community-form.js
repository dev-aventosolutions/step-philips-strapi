'use strict';

/**
 * contact-community-form service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contact-community-form.contact-community-form');
