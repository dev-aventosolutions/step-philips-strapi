'use strict';

/**
 * c-letting-violation-call router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::c-letting-violation-call.c-letting-violation-call');
