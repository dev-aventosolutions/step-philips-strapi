'use strict';

/**
 * c-letting-violation-call controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::c-letting-violation-call.c-letting-violation-call');
