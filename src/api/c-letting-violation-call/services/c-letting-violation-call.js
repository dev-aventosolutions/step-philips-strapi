'use strict';

/**
 * c-letting-violation-call service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::c-letting-violation-call.c-letting-violation-call');
