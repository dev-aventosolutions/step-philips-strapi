'use strict';

/**
 * c-letting-violation-report router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::c-letting-violation-report.c-letting-violation-report');
