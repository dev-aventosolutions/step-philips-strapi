'use strict';

/**
 * c-letting-violation-report controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::c-letting-violation-report.c-letting-violation-report');
