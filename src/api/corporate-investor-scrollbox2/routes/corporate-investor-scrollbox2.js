'use strict';

/**
 * corporate-investor-scrollbox2 router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::corporate-investor-scrollbox2.corporate-investor-scrollbox2');
