'use strict';

/**
 * investor-accreditation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::investor-accreditation.investor-accreditation');
