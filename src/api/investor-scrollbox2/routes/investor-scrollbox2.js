'use strict';

/**
 * investor-scrollbox2 router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::investor-scrollbox2.investor-scrollbox2');
