'use strict';

/**
 * investor-scrollbox2 controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::investor-scrollbox2.investor-scrollbox2');
