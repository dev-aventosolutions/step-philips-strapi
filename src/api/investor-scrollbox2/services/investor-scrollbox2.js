'use strict';

/**
 * investor-scrollbox2 service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::investor-scrollbox2.investor-scrollbox2');
