'use strict';

/**
 * equality-diversity-arr service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::equality-diversity-arr.equality-diversity-arr');
