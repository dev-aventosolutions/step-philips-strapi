'use strict';

/**
 * equality-diversity-arr controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::equality-diversity-arr.equality-diversity-arr');
