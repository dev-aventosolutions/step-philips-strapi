'use strict';

/**
 * equality-diversity-arr router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::equality-diversity-arr.equality-diversity-arr');
