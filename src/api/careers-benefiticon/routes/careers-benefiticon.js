'use strict';

/**
 * careers-benefiticon router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::careers-benefiticon.careers-benefiticon');
