'use strict';

/**
 * investor-scrollbox1 router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::investor-scrollbox1.investor-scrollbox1');
