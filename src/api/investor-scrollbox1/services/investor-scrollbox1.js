'use strict';

/**
 * investor-scrollbox1 service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::investor-scrollbox1.investor-scrollbox1');
