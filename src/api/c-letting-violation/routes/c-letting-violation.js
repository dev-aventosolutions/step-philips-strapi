'use strict';

/**
 * c-letting-violation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::c-letting-violation.c-letting-violation');
