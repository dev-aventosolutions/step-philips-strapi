'use strict';

/**
 * c-letting-violation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::c-letting-violation.c-letting-violation');
