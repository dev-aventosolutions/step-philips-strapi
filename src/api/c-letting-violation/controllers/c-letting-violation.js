'use strict';

/**
 * c-letting-violation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::c-letting-violation.c-letting-violation');
