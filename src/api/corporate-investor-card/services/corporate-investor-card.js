'use strict';

/**
 * corporate-investor-card service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::corporate-investor-card.corporate-investor-card');
