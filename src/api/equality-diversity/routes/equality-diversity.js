'use strict';

/**
 * equality-diversity router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::equality-diversity.equality-diversity');
