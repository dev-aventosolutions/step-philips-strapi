'use strict';

/**
 * equality-diversity service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::equality-diversity.equality-diversity');
