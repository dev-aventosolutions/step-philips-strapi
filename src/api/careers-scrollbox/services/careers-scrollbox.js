'use strict';

/**
 * careers-scrollbox service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::careers-scrollbox.careers-scrollbox');
